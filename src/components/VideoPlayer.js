import React from 'react'
import './VideoPlayer.css';

function VideoPlayer({ videoTitle }) {
    return (
        <div>
            <h1>Hello{videoTitle}</h1>
        </div>
    )
}

export default VideoPlayer
